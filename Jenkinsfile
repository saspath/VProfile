pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Hello World!!!'
                sh 'mvn --version'
                sh 'git --version'
            }
        }
    }
}
